const certificates=[{icon:'🏆',name:'Certificate 1',desc:'Certificate / Achievement',image:'',pdf:'1 certificate.pdf'},{icon:'🎓',name:'Certificate 2',desc:'Course / Training Certificate',image:'',pdf:'2 certificate.pdf'},{icon:'📜',name:'Certificate 3',desc:'Achievement Certificate',image:'',pdf:'3 certificate.pdf'},{icon:'⭐',name:'Certificate 4',desc:'Participation / Skill Certificate',image:'',pdf:'4 certificate.pdf'}];
const cg=document.getElementById('certificateGrid');
certificates.forEach((c,i)=>{const d=document.createElement('button');d.className='certificate-card';d.innerHTML=`<div class="certificate-icon">${c.icon}</div><h3>${c.name}</h3><p>${c.desc}</p><span class="certificate-view">📄 Click to View</span>`;d.onclick=()=>openCertificate(i);cg.appendChild(d)});
function openCertificate(i){const c=certificates[i];let body=`<div class="certificate-modal"><img src="${c.image}" alt="${c.name}"><div class="certificate-title">${c.name}</div><p>${c.desc}</p>`;if(c.pdf)body+=`<iframe class="doc-frame" src="${c.pdf}"></iframe>`;else body+=`<div class="notice">📎 Actual certificate add karne ke liye certificates.js mein image ya PDF path replace karein.</div>`;body+='</div>';openModal(c.icon+' '+c.name,'Certificate',body)}
